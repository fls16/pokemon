package pokemon.util;

public class TileTest {

    // public int region = 1;
    // public int area = 2;
    // List<Encounter> encounter_map =
    // GlobalData.route_map.get(region).getEncounterList(area);

}